input {
  tcp {
    port => 5044
    codec => json
  }
}

filter {
  # Example filter: parse Spring Boot style JSON logs if needed
  # If logs are already structured JSON, this may be optional
}

output {
  elasticsearch {
    hosts => ["http://elasticsearch:9200"]
    index => "%{service}-%{+YYYY.MM.dd}"
  }

  stdout { codec => rubydebug }
}
