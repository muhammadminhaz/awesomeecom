
# Customer Deployment
apiVersion: apps/v1
kind: Deployment
metadata:
  name: order-service
  namespace: awesomeecom
spec:
  replicas: 1
  selector:
    matchLabels:
      app: order-service
  template:
    metadata:
      labels:
        app: order-service
    spec:
      containers:
        - name: order-service
          image: order-service:1.0
          ports:
            - containerPort: 4000
          envFrom:
            - configMapRef:
                name: order-service-config
            - secretRef:
                name: order-service-secret
          resources:
            requests:
              cpu: "100m"
              memory: "256Mi"
            limits:
              cpu: "200m"
              memory: "384Mi"
---
# Customer Service
apiVersion: v1
kind: Service
metadata:
  name: order-service
  namespace: awesomeecom
spec:
  selector:
    app: order-service
  ports:
    - port: 4003
      targetPort: 4003
  type: ClusterIP
